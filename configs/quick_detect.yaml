# Quick detection experiment — single image inference
name: "quick-detection"
description: "Quick object detection on a single image"
stages: []

runtime:
  workspace: "./vw_workspace"
  device: "auto"
  seed: 42

# No pipeline stages needed — just use vw detect directly
